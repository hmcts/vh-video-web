import { RoomNameDirective } from './room-name.directive';

describe('RoomNameDirective', () => {
  it('should create an instance', () => {
    const directive = new RoomNameDirective();
    expect(directive).toBeTruthy();
  });
});
